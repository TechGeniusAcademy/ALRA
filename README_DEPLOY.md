# 🏨 ALRA Eco Village - Готов к деплою!

Проект полностью подготовлен к развертыванию на Ubuntu сервере.

## 📁 Созданные файлы для деплоя

### Конфигурационные файлы
- **ecosystem.config.js** - конфигурация PM2 для управления Node.js процессом
- **nginx.conf** - конфигурация веб-сервера Nginx
- **server/.env.production** - переменные окружения для production сервера
- **client/.env.production** - переменные окружения для production клиента
- **.gitignore** - файлы, исключаемые из git

### Скрипты деплоя (Linux/Ubuntu)
- **setup-server.sh** - первоначальная настройка Ubuntu сервера
- **deploy.sh** - основной скрипт деплоя приложения
- **update.sh** - быстрое обновление приложения
- **mysql-setup.sql** - SQL команды для настройки базы данных

### Скрипты Windows
- **deploy-from-windows.ps1** - PowerShell скрипт для загрузки на сервер

### Документация
- **DEPLOYMENT.md** - полная инструкция по деплою (НАЧНИТЕ ЗДЕСЬ! 👈)
- **DEPLOYMENT_CHECKLIST.md** - чеклист для проверки всех шагов
- **QUICK_REFERENCE.md** - краткая справка по командам
- **WINSCP_GUIDE.md** - инструкция по загрузке файлов через WinSCP
- **README_DEPLOY.md** - этот файл

## 🚀 Быстрый старт

### Данные для подключения
```
IP: 89.104.74.76
Логин: root
Пароль: yvaAaDiBKCQ8ybN4
```

### Шаги для деплоя

1. **Прочитайте полную инструкцию**: [DEPLOYMENT.md](DEPLOYMENT.md)

2. **Загрузите файлы на сервер** (любым способом):
   - WinSCP (рекомендуется) - см. [WINSCP_GUIDE.md](WINSCP_GUIDE.md)
   - PowerShell скрипт - `deploy-from-windows.ps1`
   - Git clone (если проект в репозитории)

3. **Подключитесь к серверу через SSH**:
   ```bash
   ssh root@89.104.74.76
   ```

4. **Выполните настройку сервера**:
   ```bash
   cd /root/alra
   chmod +x *.sh
   ./setup-server.sh
   ```

5. **Настройте MySQL**:
   ```bash
   sudo mysql < mysql-setup.sql
   ```
   Установите безопасный пароль!

6. **Обновите конфигурацию**:
   ```bash
   nano server/.env.production
   # Установите DB_PASSWORD
   
   nano deploy.sh
   # Установите тот же DB_PASSWORD
   ```

7. **Запустите деплой**:
   ```bash
   sudo mkdir -p /var/www/alra
   sudo cp -r /root/alra/* /var/www/alra/
   sudo chown -R $USER:$USER /var/www/alra
   cd /var/www/alra
   ./deploy.sh
   ```

8. **Проверьте результат**:
   Откройте в браузере: http://89.104.74.76

## 📚 Структура проекта

```
Alra/
├── client/                 # React frontend
│   ├── src/
│   ├── public/
│   └── package.json
├── server/                 # Node.js backend
│   ├── controllers/
│   ├── models/
│   ├── routes/
│   ├── config/
│   └── server.js
├── ecosystem.config.js     # PM2 конфигурация
├── nginx.conf             # Nginx конфигурация
├── setup-server.sh        # Скрипт настройки сервера
├── deploy.sh              # Скрипт деплоя
├── update.sh              # Скрипт обновления
└── DEPLOYMENT.md          # Полная инструкция
```

## 🛠️ Технологии

### Frontend
- React 19
- React Router DOM
- Axios
- Leaflet (карты)
- React Slick (слайдер)

### Backend
- Node.js + Express
- MySQL + Sequelize ORM
- JWT Authentication
- Multer (загрузка файлов)
- Nodemailer (email)

### DevOps
- PM2 (процесс менеджер)
- Nginx (веб-сервер)
- MySQL (база данных)

## 📝 Важные замечания

### Безопасность
1. **Обязательно измените пароль базы данных** в файлах:
   - `server/.env.production`
   - `deploy.sh`

2. После деплоя рекомендуется:
   - Настроить SSL сертификат (HTTPS)
   - Создать нового пользователя (не root)
   - Настроить автоматические бэкапы

### Требования к серверу
- Ubuntu 20.04+ или 22.04
- Минимум 1GB RAM
- Минимум 10GB свободного места
- Открытые порты: 22 (SSH), 80 (HTTP), 443 (HTTPS)

### Порты
- **80** - Nginx (frontend)
- **5001** - Node.js backend (закрыт снаружи, доступен через Nginx)
- **3306** - MySQL (только localhost)

## 🔧 Управление после деплоя

### Проверка статуса
```bash
pm2 status
sudo systemctl status nginx
sudo systemctl status mysql
```

### Просмотр логов
```bash
pm2 logs
tail -f /var/www/alra/logs/out.log
sudo tail -f /var/log/nginx/access.log
```

### Перезапуск
```bash
pm2 restart alra-backend
sudo systemctl restart nginx
```

### Обновление приложения
```bash
cd /var/www/alra
./update.sh
```

## 📞 Полезные ссылки

После деплоя ваш сайт будет доступен:
- **Frontend**: http://89.104.74.76
- **Backend API**: http://89.104.74.76/api
- **Admin Panel**: http://89.104.74.76/admin

## 📖 Документация

Для подробной информации см.:
- [DEPLOYMENT.md](DEPLOYMENT.md) - Полная инструкция по деплою
- [DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md) - Чеклист
- [QUICK_REFERENCE.md](QUICK_REFERENCE.md) - Справка по командам
- [WINSCP_GUIDE.md](WINSCP_GUIDE.md) - Загрузка через WinSCP

## ⚠️ Решение проблем

Если что-то пошло не так, проверьте:
1. Логи приложения: `pm2 logs`
2. Логи Nginx: `sudo tail -f /var/log/nginx/error.log`
3. Статус сервисов: `pm2 status`, `sudo systemctl status nginx`
4. Подключение к БД в `.env` файле

Подробнее в разделе "Решение проблем" в [DEPLOYMENT.md](DEPLOYMENT.md)

## ✅ Готово!

Все необходимые файлы созданы. Следуйте инструкции в [DEPLOYMENT.md](DEPLOYMENT.md) для деплоя.

**Успешного развертывания! 🎉**
